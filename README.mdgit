Dies ist die Perfekte Vorlage um mit einem PHP Webseiten Projekt starten zu können.

Notes:
How to Git Push -
	>git add *
	>git commit -m "first commit"
	>git branch -M main
	>git push -u origin main